services:
  postgres:
    image: 'postgres:latest'
    restart: unless-stopped
    environment:
      - 'POSTGRES_DB=movie_play_db'
      - 'POSTGRES_PASSWORD=wilson.guti'
      - 'POSTGRES_USER=wilson'
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/data/postgres
volumes:
  pgdata: